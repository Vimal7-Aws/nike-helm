apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-currencies.fullname" . }}
  labels:
    {{- include "helm-currencies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-currencies.selectorLabels" . | nindent 4 }}
