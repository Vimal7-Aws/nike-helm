apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-currencies.fullname" . }}-test-connection"
  labels:
    {{- include "helm-currencies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-currencies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
