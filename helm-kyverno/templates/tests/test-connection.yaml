apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-kyverno.fullname" . }}-test-connection"
  labels:
    {{- include "helm-kyverno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-kyverno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
